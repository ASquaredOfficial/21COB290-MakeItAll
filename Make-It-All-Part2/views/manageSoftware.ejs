<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('head') -%>
    <script defer src="/js/manage.js"></script>
    <script defer src="/js/manageSoftware.js"></script>
</head>

<body>
    <%- include('navbar') -%>
    <div id="managerPage" class ="center">
        <div id="managerHeader" class="center">
            <p id="managerTitle"> <%= pageTitle %></p>
            <% button_links.forEach(element => { %>
                <a href="<%= element.url %>" type="button" id="specialSubmitButton" class="manageButtonsDecorations pointer <%= element.underline %>" ><%= element.text %></a>
            <% }) %>
        </div>

        <div id="searchGroup" class="center" style="padding-top:0px;">
            <select id="manageSoftwareSearchDropdown" class="manageTableDropdown dropInput pointer" type="text">
                <option value="all" selected>All</option>
                <option value="id">ID</option>
                <option value="name">Software Name</option>
            </select>
            <input class="center searchInput" name="searchInput" type="text" id='manageSoftwareSearchBar' autocomplete="off" placeholder="Search" autofocus style="display: inline;"/>
            <span style="width:0px;"></span>
            <button id="searchButton" onclick="searchSoftwareTable()">Search</button>
        </div>

        <div id="editOptions">
            <div class="plus" type="submit" onclick="addSoftware()"></div>
            <div class="editButton" type="submit" onclick="showUpdatePopup()"></div>

            <form id="deleteForm" method="post" action="/admin/manage/software/delete">
                <div class="minusButton" onclick="deleteRow()"></div>
                <input id="deleteInput" name="id" class="dropInput input" type="text" oninput="returnTableBG()" placeholder="ID" list="listIds" autocomplete="off" required></input>
                    <datalist id="listIds">
                        <% for(var i=0; i < data.length; i++){ %>
                            <option value="<%= data[i].id %>">
                        <% } %>
                    </datalist>
            </form>
        </div>

        <table id="managerTable" class="center manager_table ">
            <colgroup class="tablewidth ">
                <col style="width: 30%;">
                <col style="width: 70%;">
            </colgroup>
            <thead>
                <tr>
                    <th class="manager_table">ID</th>
                    <th class="manager_table">Product Name</th>
                </tr>
                <tr>
                    <form id="addSoftwareForm" method="post" action="/admin/manage/software/post">
                        <td class="manager_table">Software ID</td>
                        <td class="manager_table">
                            <input name="name" type="text" class="dark input dropInput" style="margin:0px; width:99%; color:#F0BA00; height: 20px;" placeholder="Software Name"></input>
                        </td>
                    </form>
                </tr>
            </thead>
            <tbody>
                <% for(var i=0; i < data.length; i++){ %>
                    <tr id="soft<%= data[i].id %>" class="soft_row" onclick=getIdentifier(this)>
                        <td id="softwareId<%= data[i].id %>" class="manager_table"><%= data[i].id %></td>
                        <td id="softwareName<%= data[i].id %>"class="manager_table"><%= data[i].name %></td>
                    </tr>
                <% } %>
                <% if(data.length == 0){ %>
                    <td colspan="2" class="manager_table"style="padding: 20px 0px;">No Software To Be Found!</td>
                <% } %>
            </tbody>
        </table>
    </div>

    <div id="updateInfo">
        <div style="float: right; display: inline;"><button id="exitButton" onclick="hideUpdatePopup()"></button></div>
        <h2 class="updateSubtitle" style="margin-bottom: 10px;"><u class="gold">Update User</u></h2>

        <form id="editSoftForm" method="post" action="/admin/manage/software/put">
            <div>
                <p class="popupLabel">Hardware ID:</p>
                <input id="updateIdentifierInput" name="id" class="popupInput" type="text" list="productID" placeholder="Enter ID" autocomplete="off" required/>
                    <datalist id="productID">
                        <% for(var i=0; i < data.length; i++){ %>
                            <option value="<%= data[i].id %>"><%= data[i].name %></option>
                        <% } %>
                    </datalist>
                
                <br><br><p class="popupLabel">Software:</p>
                <input name="name" class="popupInput" type="text" placeholder="Software Name" autocomplete="off"></input>
            </div><br>
            
            <div class="txtCenter">
                <button class="filterPopupButton" onclick="editSoftwareSubmit()">Update</button>
            </div>
        </form>
    </div>

    <button id="toTopButton" onclick="topFunction()" style="display: none;"><img id="upIcon" src="/images/UpButtonIcon.png"></button>
    <%- include('footer') -%>
</body>

</html>
